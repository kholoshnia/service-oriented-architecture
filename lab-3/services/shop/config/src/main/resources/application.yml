server:
  port: 8100

spring:
  application:
    name: os-shop-config

  cloud:
    config:
      enabled: false
      server:
        git:
          uri: https://github.com/kholoshnia/soa-config.git
          clone-on-start: true

eureka:
  instance:
    prefer-ip-address: true
